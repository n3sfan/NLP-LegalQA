[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "annotate-qa"
version = "0.1.0"
dependencies = []

[project.scripts]
annotate = "annotate_qa.server:main"

[tool.hatch.build.targets.wheel]
packages = ["annotate_qa"]
